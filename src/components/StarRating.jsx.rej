diff a/src/components/StarRating.jsx b/src/components/StarRating.jsx	(rejected hunks)
@@ -5,3 +5,3 @@ const StarRating = () => {
-  const stars = [1, 2, 3, 4, 5]
-  const [rating, setRating] = useState(0)
-  const [hoverStar, setHover] = useState(null)
+  const stars = [1, 2, 3, 4, 5];
+  const [rating, setRating] = useState(0);
+  const [hoverStar, setHover] = useState(null);
@@ -10 +10 @@ const StarRating = () => {
-    <div className= 'ratingContainer' >
+    <div className="ratingContainer">
@@ -22 +22,3 @@ const StarRating = () => {
-              color={ratingValue <= (hoverStar || rating) ? "#ffc107" : "e4e6e9"}
+              color={
+                ratingValue <= (hoverStar || rating) ? '#ffc107' : 'e4e6e9'
+              }
@@ -25,2 +27,2 @@ const StarRating = () => {
-              onMouseLeave={() => setHover(null)} 
-              />
+              onMouseLeave={() => setHover(null)}
+            />
@@ -28 +30 @@ const StarRating = () => {
-        )
+        );
@@ -31,2 +33,2 @@ const StarRating = () => {
-  )
-}
+  );
+};
